apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ms-participant-managment-postgresql-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: Secret
metadata:
  name: ms-participant-managment-postgresql
  namespace: default
  labels:
    app: ms-participant-managment-postgresql
type: Opaque
data:
  postgresql-password: NDcwOTlkNDQyMTE2MGIwMzFmMzZhZDM5OGJkMDAxZTllYTIyYWM0OWZjMzE0Yjg1MzVlYjY3ZTViNDU5
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-participant-managment-postgresql
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-participant-managment-postgresql
  template:
    metadata:
      labels:
        app: ms-participant-managment-postgresql
    spec:
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: ms-participant-managment-postgresql-pvc
      containers:
        - name: postgres
          image: postgres:14.5
          env:
            - name: POSTGRES_USER
              value: ms-participant-managment
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ms-participant-managment-postgresql
                  key: postgresql-password
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
              subPath: postgres
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1"
---
apiVersion: v1
kind: Service
metadata:
  name: ms-participant-managment-postgresql
  namespace: default
spec:
  selector:
    app: ms-participant-managment-postgresql
  ports:
    - port: 5432

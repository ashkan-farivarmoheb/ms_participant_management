apiVersion: v1
kind: Service
metadata:
  name: {{.Values.application.name}}-service
  namespace: default
  labels:
    app: {{.Values.application.name}}
spec:
  selector:
    app: {{.Values.application.name}}
  ports:
    - name: http
      port: 80
      targetPort: 8090
